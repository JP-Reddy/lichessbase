<div class="col-md-3 col-sm-6 hero-feature">
    <div class="thumbnail">
        
        <div class="caption">
            <h3>Categories</h3>
                
              <ul class="list-group">
            <% Gametype.all.each do |category| %>
               <!-- <li class="<%= 'active' if params[:category] == category.name %>"> -->
               <!-- <%= link_to category.name, games_path(category: category.name), class: "link" %> -->
              
                <li class="list-group-item"><%= link_to category.name, games_path(category: category.name) %></li>
              </ul>
            <% end %>
        </div>
    </div>
</div>