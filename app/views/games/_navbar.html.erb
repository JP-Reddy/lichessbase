<nav class="navbar navbar-default">

    <div class="container">

      <div class="navbar-header">
        <%= link_to "Home", root_path, class: "navbar-brand" %>
      </div>

      <ul class="nav navbar-nav">
      </ul>

      <ul class="nav navbar-nav navbar-right">

       <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Game Categories <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% Gametype.all.each do |category| %>
              <li class="<%= 'active' if params[:gametype] == category.name %>"> 
              <li>                 
                <%= link_to category.name, games_path(gametype: category.name), class: "link" %>
              </li>
            <% end %>
          </ul>
        </li>
        
        <% if user_signed_in? %>
          <li><%= link_to "Add favorite games", new_user_favoritegame_path %></li>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>  
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
          <li><%= link_to "Log In", new_user_session_path %></li>
        <% end %>
        
      </ul>

    </div>

  </nav>